# Assistant feedback sink (Supabase). Leave empty to hide the feedback UI.
# Get both values from Supabase → Project Settings → API.
# Use the anon/publishable key ONLY — never service_role.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
